# PostgreSQL local iniciado com `docker compose up -d`.
# Não versione o arquivo .env.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/esportes?schema=public"

# Porta local da API (opcional).
PORT=3000
